void main(List<String> args) {
  // var is_login = false;
  // var user = is_login ? 'Sonam' : 'Guest';
  // print(user);

  // var name = 'Sonam';
  var name = null;
  var user = name ?? 'Guest';
  print(user);
}
